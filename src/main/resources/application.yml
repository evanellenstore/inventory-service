server:
  port: 8081

spring:
  application:
    name: inventory-service

  datasource:
    url: jdbc:mysql://localhost:3306/store_db
    username: root
    password: ellenverma

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
